# TubeMerge – YouTube Playlist Merger
# Python dependency manifest
# Install with: pip install -r requirements.txt

# ── YouTube download engine ──────────────────────────────────────────────────
yt-dlp>=2024.08.06

# ── Web server / API framework ───────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# ── Native desktop window ────────────────────────────────────────────────────
pywebview>=5.0
pythonnet>=3.0.0; sys_platform == 'win32'

# ── Image processing (thumbnail handling, banner generation) ─────────────────
pillow>=10.0.0

# ── HTTP client (used by main.py health-check polling + internal calls) ──────
httpx>=0.27.0
